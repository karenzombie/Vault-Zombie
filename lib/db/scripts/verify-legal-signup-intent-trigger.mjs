import pg from "pg";
if (!process.env.DATABASE_URL) throw new Error("DATABASE_URL is required to verify legal signup intent triggers.");
const client = new pg.Client({ connectionString: process.env.DATABASE_URL });
await client.connect(); try { const { rows } = await client.query("select tgname from pg_trigger where tgrelid = 'legal_signup_intents'::regclass and not tgisinternal and tgname in ('legal_signup_intents_consume_only','legal_signup_intents_no_delete')"); if (rows.length !== 2) throw new Error("legal signup intent lifecycle triggers are not installed."); console.log(rows.map((r) => r.tgname).join(", ")); } finally { await client.end(); }